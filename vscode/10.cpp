#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

int main() {
    /*
    int i;
    vector<int> v(10, 1);
    vector<int> v1(10, 2);
    //for (i = 0; i < v1.size(); i++) {
        //cout << v1[i] << ' ';
        // code 
    //}
    v1.insert(v1.begin()+2,v.begin(), v.end());

    vector<int>::iterator it;//=v1.begin();
    for(it=v1.begin();it!=v1.end();it++)
    {
        cout<<*it<<' ';
    }
    cout<<endl;

    for (i = 0; i < v1.size(); i++)
    {
        cout << v1[i] << ' ';
        //
    }
    */
    int a=1;
    cout<<sizeof(a)<<endl;

    return 0;
}